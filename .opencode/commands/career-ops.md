---
description: AI job search command center -- evaluate offers, generate CVs, scan portals, track applications
---

Load the career-ops skill and run in discovery mode:

```javascript
skill({ name: "career-ops" })
```

$ARGUMENTS
